---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: natsprofiles.nats.failover.io
spec:
  group: nats.failover.io
  names:
    kind: NatsProfile
    listKind: NatsProfileList
    plural: natsprofiles
    singular: natsprofile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsProfile is the Schema for the natsprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NatsProfileSpec defines the desired state of NatsProfile
            properties:
              activeBroker:
                type: string
              brokers:
                items:
                  properties:
                    domain:
                      type: string
                    endpoint:
                      type: string
                    name:
                      type: string
                  required:
                  - domain
                  - endpoint
                  - name
                  type: object
                type: array
              gateway:
                properties:
                  configMapName:
                    type: string
                  deploymentName:
                    type: string
                  namespace:
                    type: string
                required:
                - configMapName
                - deploymentName
                - namespace
                type: object
              streamName:
                type: string
              subject:
                type: string
              thresholds:
                properties:
                  activeFailureSeconds:
                    format: int32
                    type: integer
                  maxLag:
                    format: int64
                    type: integer
                type: object
            required:
            - activeBroker
            - brokers
            - gateway
            - streamName
            - subject
            type: object
          status:
            description: NatsProfileStatus defines the observed state of NatsProfile.
            properties:
              message:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
